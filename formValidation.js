const name = document.getElementById('log_login');
const pass = document.getElementById('log_password');
const form = document.getElementById('logowanie_form');
// const form = document.getElementById('logowanie_form');
// const form = document.getElementById('logowanie_form');

form.addEventListener('submit', (e) =>{
    let messages = []
    if (name.value ===''||){}
    e.preventDefault()
})